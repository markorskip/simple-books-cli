package io.efficientsoftware.simplebookscli.transaction;

import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;


import io.efficientsoftware.simplebookscli.transaction.model.SimpleTransaction;

public class ReportService {
	
	
	//public List<String> getAmountsByCategory(List<SimpleTransaction> list) {
//		Map<String, Double> result = new HashMap<>();
//		
//		for (SimpleTransaction item : list) {
//			result.computeIfAbsent(item.category, r-> r + item.amount);
//			result.putIfAbsent(item.category, item.amount);
//		}
//		
//		return result.keySet().stream().map(x-> x + ": " result.get(x)).collect(Collectors.toList());
	//}

}
