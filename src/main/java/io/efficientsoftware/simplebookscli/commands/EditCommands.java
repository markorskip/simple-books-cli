package io.efficientsoftware.simplebookscli.commands;

public class EditCommands {
    // IMPORTANT NOTE: Edit really just finds the original, creates a new one off of it with updated values, deletes the original and creates a new
    // No event is actually edited in place
}
